# Hermes Desktop Mod Next Steps

1. Smoke-test the desktop UI visually and confirm the new HermesOps section renders as expected.
2. Wire a safe local bridge later if the app gains an allowlisted command-execution API for read-only `hermesops` calls.
3. Re-run the Windows/WSLg keyboard path after setting Windows to ABNT2 and restarting WSL.
4. Clean up the unrelated pre-existing lint and UI test failures in the desktop repo.
5. If the PT-BR flow is confirmed, document the exact launch steps for the Windows users who need it.

