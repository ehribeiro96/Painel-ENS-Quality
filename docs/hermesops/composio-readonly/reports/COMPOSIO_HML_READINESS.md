# Prontidão HML do Composio

Falta para HML:
- credenciais fora do Git;
- autenticação manual aprovada;
- allowlist final;
- auditoria;
- rollback;
- validação read-only e draft-only;
- revisão de audit logs e sanitized logs;
- exclusão de `reports/desktop_cli/runtime_logs/*.jsonl`;
- exclusão de `reports/composio_plugin/runtime_logs/*.jsonl`;
- exclusão de `reports/composio_plugin/audit_logs/*.jsonl`;
- exclusão de `reports/composio_plugin/sanitized_logs/*.jsonl` salvo se sintético e aprovado.
